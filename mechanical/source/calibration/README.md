Add a README.md in that folder explaining each test, the parameters they depend on (e.g., HoleClearance, InsertOD), and how to interpret results.

With this parameter set, you can switch materials, printers, or variants by editing one params file, then run your headless script to recompute and re‑export all parts automatically.

